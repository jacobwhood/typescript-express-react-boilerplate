{
    "name": "server",
    "version": "1.0.0",
    "description": "TS, Express, React boilerplate",
    "repository": "https://github.com/jacobwhood/V-streamY",
    "license": "UNLICENSED",
    "main": "dist/app.js",
    "keywords": [],
    "author": "Jacob Hood",
    "scripts": {
        "prebuild": "tslint -c tslint.json -p tsconfig.json --fix",
        "build": "npm run prebuild && tsc",
        "prestart": "npm run build",
        "start": "ts-node dist/app.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "dependencies": {
        "express": "^4.16.4",
        "ts-node": "^8.0.2"
    },
    "devDependencies": {
        "@types/express": "^4.16.1",
        "@types/node": "^10.12.21",
        "dotenv": "^8.2.0",
        "tslint": "^5.12.1",
        "typescript": "^2.8.3"
    }
}
